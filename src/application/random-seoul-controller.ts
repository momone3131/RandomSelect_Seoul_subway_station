import { getCuratedAttractions } from '../data/curated-attractions';import { FOOD_BY_ID } from '../data/food-categories';import { SUBWAY_LINE_BY_ID } from '../data/subway-lines';import { drawOne,type RandomSource } from '../domain/draw-engine';import { rankRestaurants } from '../domain/restaurant-ranking';import type { DrawHistoryItem,FoodCategory,PlaceCandidate,RestaurantRecommendation,SubwayLine } from '../domain/types';import type { AppStore } from '../state/app-state';import type { PlaceSearchService } from '../services/places/place-search';import type { StationLocationService } from '../services/places/station-location';const FOOD_PLACE_TYPES=new Set(['restaurant','cafe','bakery','meal_takeaway','meal_delivery','bar','food']);export type DrawStage='line'|'station'|'food'|'done';export interface DrawResult{stage:DrawStage;recommendations?:RestaurantRecommendation[];}
export class RandomSeoulController{private activeHistoryId?:string;private sequence=0;constructor(readonly store:AppStore,private readonly places:PlaceSearchService,private readonly stationLocations:StationLocationService,private readonly random:RandomSource=Math.random,private readonly now:()=>number=Date.now){}getStage():DrawStage{const s=this.store.getSnapshot();if(!s.currentLine)return'line';if(!s.currentStation)return'station';if(!s.currentFood)return'food';return'done';}async drawNext():Promise<DrawResult>{const stage=this.getStage();if(stage==='done'){this.store.resetCourse();this.activeHistoryId=undefined;return this.drawLineResult();}if(stage==='line')return this.drawLineResult();if(stage==='station')return this.drawStationResult();return this.drawFoodResult();}redrawLine():DrawResult{this.store.resetCourse();this.activeHistoryId=undefined;return this.drawLineResult();}redrawStation():DrawResult{const s=this.store.getSnapshot();if(!s.currentLine)return this.drawLineResult();this.store.update(c=>({...c,currentStation:undefined,currentFood:undefined,attractions:[],recommendations:[]}));this.activeHistoryId=undefined;return this.drawStationResult();}redrawFood():DrawResult{const s=this.store.getSnapshot();if(!s.currentLine)return this.drawLineResult();if(!s.currentStation)return this.drawStationResult();this.store.resetFood();return this.drawFoodResult();}setSelectedLines(ids:readonly string[]):void{const valid=ids.filter(id=>SUBWAY_LINE_BY_ID.has(id));if(!valid.length)throw new RangeError('At least one subway line must be selected.');this.store.update(s=>({...s,preferences:{...s.preferences,selectedLineIds:[...valid]},currentLine:undefined,currentStation:undefined,currentFood:undefined,attractions:[],recommendations:[]}));this.activeHistoryId=undefined;}setSelectedFoods(ids:readonly string[]):void{const valid=ids.filter(id=>FOOD_BY_ID.has(id));if(!valid.length)throw new RangeError('At least one food category must be selected.');this.store.update(s=>({...s,preferences:{...s.preferences,selectedFoodIds:[...valid]},currentFood:undefined,recommendations:[]}));}setInstantDraw(enabled:boolean):void{this.store.update(s=>({...s,preferences:{...s.preferences,instantDraw:enabled}}));}clearHistory():void{this.activeHistoryId=undefined;this.store.update(s=>({...s,history:[]}));}private drawLineResult():DrawResult{const s=this.store.getSnapshot();const candidates=s.preferences.selectedLineIds.map(id=>SUBWAY_LINE_BY_ID.get(id)).filter((line):line is SubwayLine=>Boolean(line));const line=drawOne(candidates,this.random);this.store.update(c=>({...c,currentLine:line,currentStation:undefined,currentFood:undefined,attractions:[],recommendations:[]}));return{stage:'line'};}private drawStationResult():DrawResult{const s=this.store.getSnapshot();if(!s.currentLine)return this.drawLineResult();const station=drawOne(s.currentLine.stations,this.random);const attractions=getCuratedAttractions(s.currentLine.id,station.name);this.activeHistoryId=this.nextHistoryId();const historyItem:DrawHistoryItem={id:this.activeHistoryId,lineId:s.currentLine.id,stationName:station.name,stationOrdinal:station.ordinal};this.store.update(c=>({...c,currentStation:station,currentFood:undefined,attractions,recommendations:[],history:[historyItem,...c.history].slice(0,12)}));return{stage:'station'};}private drawFoodResult():DrawResult{const s=this.store.getSnapshot();if(!s.currentLine)return this.drawLineResult();if(!s.currentStation)return this.drawStationResult();const foods=s.preferences.selectedFoodIds.map(id=>FOOD_BY_ID.get(id)).filter((food):food is FoodCategory=>Boolean(food));const food=drawOne(foods,this.random);this.store.update(c=>({...c,currentFood:food,recommendations:[]}));this.attachFoodToHistory(food.id);return{stage:'food'};}async loadRecommendations():Promise<RestaurantRecommendation[]>{const s=this.store.getSnapshot(),line=s.currentLine,station=s.currentStation,food=s.currentFood;if(!line||!station||!food)return[];const center=await this.stationLocations.resolve(line,station.name);const query=`${station.name}역 ${food.searchQuery??food.name}`;const candidates=await this.places.searchText({textQuery:query,center:{latitude:center.latitude,longitude:center.longitude},radiusMeters:2000,maxResults:20,language:'ko',region:'kr'});const recommendations=rankRestaurants(candidates.filter(c=>this.isFoodCandidate(c,food)),center);this.store.update(c=>({...c,recommendations}));return recommendations;}private isFoodCandidate(candidate:PlaceCandidate,food:FoodCategory):boolean{const types=candidate.types??[];if(!types.length)return true;if(food.id==='w_brunch'&&types.includes('cafe'))return true;return types.some(type=>FOOD_PLACE_TYPES.has(type));}private attachFoodToHistory(foodId:string):void{const id=this.activeHistoryId;this.store.update(c=>!id?c:{...c,history:c.history.map(item=>item.id===id?{...item,foodId}:item)});}private nextHistoryId():string{this.sequence+=1;return`${this.now()}-${this.sequence}`;}}
