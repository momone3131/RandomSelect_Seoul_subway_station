{
  "name": "random-seoul",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:legacy": "node scripts/extract-legacy-shell.mjs",
    "dev": "npm run generate:legacy && vite",
    "build": "npm run generate:legacy && tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
